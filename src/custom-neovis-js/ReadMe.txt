There is a customization done on the library neovis.js 1.6.0 (nevis.js-master.zip)

Changed files are nevis.js and package.json
distribution after customization is in the dist folder

appcodes package.json file has the npm script which will trigger before the start trigger to copy the dist/neovis.js into node_modules/neovis.js/dist